# web-dashboard / explanation

Understanding-oriented. Background, concepts, why things work the way they do. Diataxis 'explanation' category.

Scope: the web dashboard.

Authored here, in `Contextual/docs/Public/`. Synced automatically to
`Contextual-Labs` on merge to main — never hand-edit the copy over there.

Both pages authored: how website login relates to CLI login, what Solo
actually includes today vs. what's planned. Scoped against `Teams/Docs/
web-docs.md`'s 2026-07-23 ground-truth audit.
