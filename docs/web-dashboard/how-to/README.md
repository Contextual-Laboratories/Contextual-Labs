# web-dashboard / how-to

Problem-oriented. Task-specific recipes for accomplishing something specific (e.g. "how to set up multi-workspace mode"). Diataxis 'how-to guide' category.

Scope: the web dashboard.

Authored here, in `Contextual/docs/Public/`. Synced automatically to
`Contextual-Labs` on merge to main — never hand-edit the copy over there.

All 3 pages authored: starting/subscribing to the trial, managing plan
and devices, linking a device via the browser. Scoped against
`Teams/Docs/web-docs.md`'s 2026-07-23 ground-truth audit.
