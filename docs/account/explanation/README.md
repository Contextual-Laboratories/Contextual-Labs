# account / explanation

Understanding-oriented. How node-locked licensing actually works here —
offline validation, one-time activation, grace periods.

Authored here, in `Contextual/docs/Public/`. Synced automatically to
`Contextual-Labs` on merge to main — never hand-edit the copy over there.

`how-licensing-works-here.md` is authored.
