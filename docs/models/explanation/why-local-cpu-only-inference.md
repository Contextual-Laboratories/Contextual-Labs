---
title: Why local, CPU-only inference
domain: models
category: explanation
tldr: "Why embedding inference runs locally on CPU via ONNX with no network calls, and what that trades off."
---

# Why local, CPU-only inference

> **Status:** structural placeholder — this page's frontmatter (title, domain,
> category, TL;DR) is locked and real; the body content below is not yet
> written. 

Content pending.
