# CLI docs

Documentation for the Contextual Python CLI (`contextual-engine` on PyPI).
Synced from `Contextual/docs/Public/cli/` — see [`../README.md`](../README.md)
for the sync model and category definitions.
