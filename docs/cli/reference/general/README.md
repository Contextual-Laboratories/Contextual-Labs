# General commands

Core lifecycle commands: workspace setup, indexing, auth, diagnostics,
install/uninstall. Start here if you're not sure which family you need.

`init`, `index`, `activity`, `login`, `logout`, `account`, `doctor`,
`fetch`, `setup`, `uninstall`, `hooks-install`, `hooks-uninstall`,
`stats`, `version`.
