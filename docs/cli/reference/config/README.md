# config commands

Read/reset the on-disk config file. See [configuration/](../../../configuration/README.md) for what each key controls.

`config` (open in editor), `config reset` (restore defaults, preserving
workspace identity).
