# workspace commands

Register, list, and remove workspaces known to the daemon — the
multi-workspace registry that lets one MCP daemon serve more than one
repository at a time.

`workspace list`, `workspace add`, `workspace rm`.
