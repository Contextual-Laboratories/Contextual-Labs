# mcp commands

Start, stop, restart, and inspect the local MCP daemon process. See [mcp/server/](../../../mcp/server/README.md) for how the daemon itself behaves.

`mcp status`, `mcp start`, `mcp stop`, `mcp restart`, `mcp logs`.
