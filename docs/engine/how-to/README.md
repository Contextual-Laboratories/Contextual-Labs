# engine / how-to

Problem-oriented, terse. Sizing a machine before a large index, speeding
up or debugging a slow run, restarting the daemon after a code vs.
config change, reading logs, excluding files with `.contextualignore`,
interpreting a `doctor` report, understanding stale/missing results.

No content yet; structure only, content comes in a later pass.
