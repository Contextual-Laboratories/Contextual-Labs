# engine / explanation

Understanding-oriented: architecture overview, how indexing works,
incremental vs. scheduled indexing, the knowledge graph, temporal
intelligence, the retrieval pipeline, nexus enrichment, the embedding/
model stack (conceptual only), MCP daemon lifecycle, and MCP client
access control.

`architecture-overview.md` is authored. The rest come in a later pass.
