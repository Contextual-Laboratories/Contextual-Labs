# engine / observability

Where logs live, how to read them, what a `doctor` report's checks
actually mean, and what telemetry is and isn't collected.

`interpreting-doctor-report.md` is authored. The rest (log locations in
full, telemetry/data-privacy detail) come in a later pass.
