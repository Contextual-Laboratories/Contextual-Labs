# Changelog

Release notes for Contextual, starting at v1.0.0 — everything before
launch was pre-release churn, not part of the public story.

This page is intro copy only, hand-authored. The version list itself is
generated on the website from each release page's frontmatter, not from
this file — see `scripts/draft_changelog.py` for how individual
`vX.Y.Z.md` pages get drafted on each release tag.
