# Integrations

Connecting, switching, and removing AI-client integrations — one page per supported client.

- `integrations/explanation/client-categories-overview` — the three
  real integration mechanisms across all 11 clients, and where
  tool-usage guidance actually lives for each.
- `integrations/tutorials/connecting-an-ai-client` — connecting a
  client to a workspace, start to finish.
- `integrations/tutorials/connect-the-mcp-server-to-your-ai-client` —
  what `contextual client` actually writes, and why the MCP server is
  a separate binary from the CLI.
- `integrations/how-to/switch-or-add-a-second-ai-client`.
- `integrations/how-to/remove-a-client-integration`.
- Reference, one page per client: `claude-desktop`, `claude-code`,
  `chatgpt`, `perplexity`, `gemini-cli`, `cursor`, `github-copilot`,
  `windsurf`, `codex`, `antigravity`, `cline`.
