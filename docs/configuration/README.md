# Configuration

Every config.toml key, precedence rules, and what each one actually controls.

- `configuration/explanation/config-precedence` — the two config layers
  (global vs. per-workspace) and why workspace identity survives a reset.
- `configuration/how-to/reset-config-to-defaults` — running `contextual
  config reset` without orphaning your indexed data.
- `configuration/reference/configuration-reference` — every real
  `config.toml` key, its default, and what it controls.
- `configuration/reference/config-key-index` — a flat key-name lookup
  into the subsystem that actually owns each setting.
